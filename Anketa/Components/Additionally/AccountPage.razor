@page "/account"
@rendermode InteractiveServer
@using Anketa.Services
@using Microsoft.AspNetCore.Components.Authorization
@using System.Security.Claims
@inject AuthenticationStateProvider AuthStateProvider
@inject NavigationManager Navigation
@inject IJSRuntime JSRuntime
@inject UserServices UserServices

<AuthorizeView>
    <Authorized>
        <div class="account-page">
            <div class="account-card">
                <div class="account-header">
                    <div class="avatar-section">
                        <div class="avatar">
                            <i class="bi bi-person-circle"></i>
                        </div>
                        <h1>Мой профиль</h1>
                    </div>
                </div>

                <div class="account-info">
                    <div class="info-item">
                        <span class="info-label">
                            <i class="bi bi-person"></i> Имя пользователя
                        </span>

                        @if (!editName)
                        {
                            <div class="display-wrapper">
                                <span class="info-value">@userName</span>
                                <button class="btn btn-dark btn-sm btn-editing-name" @onclick="EditNameView">Изменить</button>
                            </div>
                        }
                        @if (editName)
                        {
                            <div class="edit-wrapper">
                                <input type="text" @bind="tempUserName" class="edit-input" />
                                <div class="edit-buttons">
                                    <button class="btn btn-dark btn-sm" @onclick="SaveUserName">Сохранить</button>
                                    <button class="btn btn-dark btn-sm" @onclick="CancelEdit">Отмена</button>
                                </div>
                            </div>
                        }
                    </div>

                    <div class="info-item">
                        <span class="info-label">
                            <i class="bi bi-envelope"></i> Email
                        </span>
                        <span class="info-value">@userEmail</span>
                    </div>

                    <div class="info-item">
                        <span class="info-label">
                            <i class="bi bi-shield-check"></i> Роль
                        </span>
                        <span class="info-value role-badge">@userRole</span>
                    </div>

                    <div class="info-item">
                        <span class="info-label">
                            <i class="bi bi-key"></i> ID пользователя
                        </span>
                        <span class="info-value">@userId</span>
                    </div>
                </div>
            </div>
        </div>
    </Authorized>
</AuthorizeView>

@code {
    private bool editName = false;
    private bool isLoading = false;
    private string userName = "";
    private string tempUserName = "";
    private string userEmail = "";
    private string userRole = "";
    private int userId = 0;

    protected override async Task OnInitializedAsync()
    {
        await LoadUserData();
    }

    private async Task LoadUserData()
    {
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;

        if (user?.Identity?.IsAuthenticated == true)
        {
            userName = user.Identity.Name ?? "Не указано";
            tempUserName = userName;

            var userIdString = user.FindFirst("UserId")?.Value
                             ?? user.FindFirst(ClaimTypes.NameIdentifier)?.Value;

            if (int.TryParse(userIdString, out int parsedId))
            {
                userId = parsedId;
            }

            userEmail = user.FindFirst(ClaimTypes.Email)?.Value ?? "Не указано";
            userRole = user.FindFirst(ClaimTypes.Role)?.Value ?? "Пользователь";
        }
    }

    private void EditNameView()
    {
        editName = !editName;
        if (editName)
        {
            tempUserName = userName;
        }
    }

    private void CancelEdit()
    {
        editName = false;
        tempUserName = userName;
    }

    private async Task SaveUserName()
    {
        if (string.IsNullOrWhiteSpace(tempUserName))
        {
            await JSRuntime.InvokeVoidAsync("alert", "Имя пользователя не может быть пустым");
            return;
        }

        if (tempUserName == userName)
        {
            editName = false;
            return;
        }

        isLoading = true;

        try
        {
            UserServices.EditUserName(userId, tempUserName);

            userName = tempUserName;
            editName = false;

            await JSRuntime.InvokeVoidAsync("alert", "Имя пользователя успешно обновлено");
            Logout();
        }
        catch (Exception ex)
        {
            await JSRuntime.InvokeVoidAsync("alert", $"Ошибка: {ex.Message}");
            tempUserName = userName;
        }
        finally
        {
            isLoading = false;
        }
    }

    private async Task Logout()
    {
        await JSRuntime.InvokeVoidAsync("localStorage.removeItem", "auth_user_session");

        await JSRuntime.InvokeVoidAsync("eval",
            "document.cookie = 'parser-auth=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;';");

        Navigation.NavigateTo("/", true);
    }
}


<style>

    .info-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 15px 0;
        border-bottom: 1px solid #4a5568;
    }

    .info-label {
        color: #a0aec0;
        font-weight: 500;
        display: flex;
        align-items: center;
        gap: 8px;
        flex-shrink: 0;
    }

    .display-wrapper {
        display: flex;
        align-items: center;
        gap: 20px;
    }

    .info-value {
        color: #ffffff;
        font-weight: 500;
        font-size: 16px;
        margin-right: 20px;
    }

    .btn-editing-name {
        background: linear-gradient(135deg, #ed8936 0%, #dd6b20 100%);
        color: white;
        border: none;
        border-radius: 6px;
        padding: 6px 16px;
        font-size: 14px;
        transition: all 0.3s;
    }

        .btn-editing-name:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(237, 137, 54, 0.3);
        }

    .edit-wrapper {
        display: flex;
        flex-direction: column;
        gap: 10px;
        width: 300px;
    }

    .edit-input {
        width: 100%;
        padding: 10px 12px;
        border: 2px solid #ed8936;
        border-radius: 6px;
        background: #2d3748;
        color: white;
        font-size: 16px;
    }

    .edit-buttons {
        display: flex;
        gap: 10px;
    }

    .btn-editing-name {
        min-width: 25px;
    }

    .btn-editing {
        margin-top: auto;
    }

    .account-page {
        min-height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    }

    .account-card {
        background: white;
        border-radius: 16px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        width: 100%;
        max-width: 600px;
        overflow: hidden;
        animation: fadeIn 0.5s ease;
    }

    .account-header {
        background: linear-gradient(135deg, #1a1a1a 0%, #2d3748 100%);
        padding: 2rem;
        color: white;
    }

    .avatar-section {
        display: flex;
        align-items: center;
        gap: 1.5rem;
    }

    .avatar {
        width: 80px;
        height: 80px;
        background: rgba(255,255,255,0.2);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        border: 3px solid white;
    }

        .avatar i {
            font-size: 3rem;
        }

    .account-header h1 {
        margin: 0;
        font-size: 1.8rem;
        font-weight: 600;
    }

    .account-info {
        padding: 2rem;
    }

    .info-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem 0;
        border-bottom: 1px solid #eee;
    }

        .info-item:last-child {
            border-bottom: none;
        }

    .info-label {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: #666;
        font-size: 0.95rem;
    }

        .info-label i {
            color: #667eea;
        }

    .info-value {
        font-weight: 500;
        color: #333;
        font-size: 1rem;
    }

    .role-badge {
        background: linear-gradient(135deg, #ed8936 0%, #dd6b20 100%);
        color: white;
        padding: 0.3rem 1rem;
        border-radius: 20px;
        font-size: 0.9rem;
        font-weight: 600;
    }

    .account-actions {
        padding: 1.5rem 2rem;
        background: #f8f9fa;
        border-top: 1px solid #eee;
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .btn-action {
        padding: 0.8rem 1.5rem;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 500;
        font-size: 0.95rem;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        transition: all 0.3s ease;
    }

    .edit-btn {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        box-shadow: 0 2px 10px rgba(102, 126, 234, 0.3);
    }

    .logout-btn {
        background: white;
        color: #dc3545;
        border: 2px solid #dc3545;
    }

    .edit-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }

    .logout-btn:hover {
        background: #dc3545;
        color: white;
        transform: translateY(-2px);
    }

    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @@media (max-width: 640px) {


        .account-page {
            padding: 1rem;
        }

        .account-header {
            padding: 1.5rem;
        }

        .avatar-section {
            flex-direction: column;
            text-align: center;
            gap: 1rem;
        }

        .info-item {
            flex-direction: column;
            align-items: flex-start;
            gap: 0.5rem;
        }

        .account-actions {
            flex-direction: column;
        }

        .btn-action {
            justify-content: center;
        }
    }
</style>
